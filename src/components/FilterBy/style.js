import styled from 'styled-components';

const ContainerFilterBy = styled.div`
  @media (max-width: 600px) {
    display: flex;
    flex-direction: column;
    margin-bottom: 30px;
  }
`;

export default ContainerFilterBy;