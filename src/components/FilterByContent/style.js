import styled from 'styled-components';

const StyledFilterByContent = styled.div`
  @media (max-width: 600px) {
    margin-bottom: 30px;
    display: flex;
    flex-direction: column;
  }
`;

export default StyledFilterByContent;